{
    "installCommand": "cd ../.. && yarn install --frozen-lockfile",
    "buildCommand": "cd ../.. && yarn workspace @repo/database run db:push && yarn workspace admin build",
    "outputDirectory": ".next",
    "framework": "nextjs"
}