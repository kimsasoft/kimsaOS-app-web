{
    "installCommand": "cd ../.. && yarn install --frozen-lockfile",
    "buildCommand": "cd ../.. && yarn workspace @repo/database run db:generate && yarn workspace admin build",
    "outputDirectory": ".next",
    "framework": "nextjs"
}